extends layout

block content
    div.row
        div.col-md-6.col-md-offset-3
            h2 User: #{user.id}
            - if (user.group === 'admin')
                p (admin)
            h2 Airport: #{user.airport}
